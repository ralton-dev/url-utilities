apiVersion: v1
kind: Service
metadata:
  name: {{ include "url-utilities.fullname" . }}
  labels:
    {{- include "url-utilities.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "url-utilities.selectorLabels" . | nindent 4 }}
